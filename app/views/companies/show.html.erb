<%= provide(:title, "#{@company.name} (#{@company.token.name})") %>
<div class="wrapper">
	<div class="page-header page-header-xs" data-parallax="true" style="background-image: url(http://assets.ethereumgrow.com/<%= @company.image %>);">
		
		<div class="filter"></div>
	</div>
	<div class="section profile-content">
		<div class="container">
			<div class="owner">
				<div class="avatar">
					<img src="http://assets.ethereumgrow.com/<%= @company.avatar %>" alt="<%= @company.name %> avatar" class="img-circle img-no-padding img-responsive">
				</div>
				<div class="name">
					<h4 class="title"><%= @company.name %> <small><strong>(<%= @company.token.name %>)</strong><br />$<%= number_to_human(@company.ico_size, strip_insignificant_zeros: true, precision: 5) %> MM ICO</small></h4>
				</div>
			</div>
			<div class="row">
				<div class="col-md-6 offset-md-3 text-center">
          <p><%= @company.description %> </p>
          <br />
					<%= link_to "#{@company.white_paper}", class: "btn btn-outline-default btn-round", target: "_blank" do %>
					<i class="fa fa-paper-plane"></i> White paper
					<% end %>
					<%= link_to "#{@company.website}", class: "btn btn-outline-default btn-round", target: "_blank" do %>
					<i class="fa fa-globe"></i> Website
					<% end %>
				</div>
			</div>
			<% if @company.video.present? %>
			<div class="row">
				<div class="col-md-8 offset-md-2 pt-4">
					<div class="embed-responsive embed-responsive-16by9">					
  					<iframe class="embed-responsive-item" src="<%= @company.video %>" allowfullscreen></iframe>
					</div>
				</div>
			</div>
			<% end %>
			
			<% if @charts.present? %>			
			<div class="row">
				<% @charts.each do |chart| %>				
				<div class="pt-3 pb-5" style="height: 415px; width: 100%;">	
					<div class="col-md-8 offset-md-2 h-100 d-inline-block">				
						<h3 class="pb-2"><%= chart.currency_name.upcase.insert(-4, '/') %></h3>
					
						<script type="text/javascript" src="https://s3.tradingview.com/tv.js"></script>
						<script type="text/javascript">
						new TradingView.widget({
						  "autosize": true,
						  "symbol": "<%= chart.exchange_name.upcase %>:<%= chart.currency_name.upcase %>",
						  "interval": "D",
						  "timezone": "Etc/UTC",
						  "theme": "Light",
						  "style": "1",
						  "locale": "en",
						  "toolbar_bg": "#f1f3f6",
						  "enable_publishing": false,
						  "hide_top_toolbar": true,
						  "save_image": false,
						  "hideideas": true
						});
						</script>	
					
					</div>			
				</div>
				<% end %>
			</div>
			<% end %>
		
		</div>
	</div>
</div>