
  <ol class="breadcrumb">
    <li class="breadcrumb-item">
      <%= link_to 'Exchanges', admin_exchanges_path %>
    </li>
    <li class="breadcrumb-item active">Exchanges</li>
  </ol>
	<%= link_to 'New Exchange', new_admin_exchange_path, class: "btn btn-success mb-3" %>
  <div class="table-responsive">
    <table class="table table-bordered table-striped table-hover" width="100%" cellspacing="0">
		  <thead>
		    <tr>
		      <th>Name</th>
		      <th>Location</th>
		      <th>Website</th>
		      <th>Actions</th>
		    </tr>
		  </thead>

		  <tbody>
		    <% @exchanges.each do |exchange| %>
		      <tr>
		        <td><%= exchange.name %></td>
		        <td><%= exchange.location %></td>
		        <td><%= link_to exchange.website, exchange.website, target: "_blank" if exchange.website %></td>
		        <td><%= link_to 'View', admin_exchange_url(exchange) %> | <%= link_to 'Edit', edit_admin_exchange_path(exchange) %> | <%= link_to 'Delete', admin_exchange_url(exchange), method: :delete, data: { confirm: 'Are you sure?' } %></td>

		      </tr>
		    <% end %>
		  </tbody>
		</table>




			